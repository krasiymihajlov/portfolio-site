import { initializeApp } from 'firebase/app';
import { getFirestore, collection, query, where, getDocs, updateDoc, Timestamp } from 'firebase/firestore';
import { readFileSync } from 'fs';
import { resolve, dirname } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));

function loadEnv() {
  const envPath = resolve(__dirname, '../.env.local');
  const content = readFileSync(envPath, 'utf-8');
  const env = {};
  for (const line of content.split('\n')) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith('#')) continue;
    const [key, ...rest] = trimmed.split('=');
    env[key.trim()] = rest.join('=').trim().replace(/^["']|["']$/g, '');
  }
  return env;
}

const env = loadEnv();
const app = initializeApp({
  apiKey: env.NEXT_PUBLIC_FIREBASE_API_KEY,
  authDomain: env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: env.NEXT_PUBLIC_FIREBASE_APP_ID,
});
const db = getFirestore(app);

const updatedContent = `Едно време планирах ден по ден. Събуждах се, отварях списъка със задачи и се захващах с работа. Изглеждаше логично — правиш нещо всеки ден, нещата се движат напред. Но в края на седмицата нещо ме притесняваше: усещах, че съм бил зает, но не и че съм постигнал нещо важно. Задачите свършваха, но приоритетите — не.

Промяната дойде, когато спрях да гледам само един ден напред и започнах да планирам цяла седмица.

---

## Дневното планиране не е достатъчно

Проблемът с дневното планиране не е, че е лошо. Проблемът е, че е непълно.

Когато гледаш само деня пред себе си, е лесно да изпадаш в капана на спешното. Имейлът, който трябва да се отговори веднага. Обаждането, което не може да чака. Малката задача, която изглежда бърза. И в края на деня — списъкът с наистина важните неща не е докоснат.

Стивън Кови описва това с концепцията за Квадрантите на времето в книгата си „7-те навика на високоефективните хора". Квадрант 2 е зоната на неспешните, но важни задачи — стратегическото мислене, личното развитие, отношенията, дългосрочните цели. Именно там се случва реалното развитие. Но без съзнателно планиране никога не стигаш до него, защото Квадрант 1 — спешното и важното — винаги изглежда по-неотложен.

Седмичното планиране е инструментът, с който влизаш в Квадрант 2 — не по случайност, а по избор.

---

## Стъпка 1: Определи ролите си

Преди да планираш задачите, трябва да знаеш кой си в контекста на тази седмица.

Всеки от нас играе множество роли — едновременно и в различни сфери на живота. Ето примерен списък с роли:

- Лично развитие
- Съпруг / партньор
- Баща / майка
- Специалист / фрийлансър
- Предприемач
- Писател / творец
- Ментор или лидер в екип

Ролите не са произволни категории. Те са напомняне, че животът не е само работа — и че ако пренебрегнеш дадена роля за достатъчно дълго, ще усетиш последствията.

Задачата е проста: преди да започне седмицата, запиши ролите си. Не е нужно да са много — три до пет са напълно достатъчни. Целта е да имаш пред очи цялата картина, а не само работния й ъгъл.

---

## Стъпка 2: Избери целите за всяка роля

Когато знаеш ролите си, следващата стъпка е да определиш два или три важни резултата за всяка от тях за следващите седем дни.

Обърни внимание — не „задачи", а **резултати**. Разликата е важна.

Задача: „Да изпратя имейл до клиента."
Резултат: „Да договорим старта на новия проект до петък."

Резултатът ти казва накъде отиваш. Задачата — само какво правиш.

В идеалния случай тези седмични цели са свързани с по-дългосрочни такива. Аз актуализирам дългосрочните си цели веднъж годишно — и всяка седмица проверявам дали това, което планирам, ме приближава към тях. Ако не — или ги оставям настрана, или преосмислям самите дългосрочни цели.

Именно тук седмичното планиране се отплаща най-силно: то те принуждава да бъдеш честен сам със себе си. Имаш ли реалистично очакване за това, което можеш да постигнеш за 7 дни? Кои задачи трябва да делегираш? Кои трябва да изчезнат от списъка?

> Времето е единственият ресурс, който не можеш да си върнеш. Седмичното планиране е начинът да решиш предварително как ще го похарчиш.

---

## Стъпка 3: Изготви реален график

Третата стъпка е да прехвърлиш целите в конкретни времеви блокове.

Не всяка цел принадлежи на работната седмица. Някои неща — лично развитие, семейство, почивка — имат своето място в събота или неделя. Неделята е особено подходяща за стимулиращи дейности и за самото организиране на следващата седмица.

Когато за пръв път започнах да правя това, ми отне около два часа. Беше непривично и изглеждаше като загуба на време. Но с практиката процесът се ускори — и сега отнема около 20-30 минути. А ефектът е несъпоставим: влизам в понеделника с яснота, а не с тревожност.

Ето какво проверявам, докато изготвям графика:

- Имам ли достатъчно работно време за приоритетните цели?
- Оставил ли съм буфер за неочаквани неща?
- Разпределил ли съм задачи, които могат да се делегират?
- Поставил ли съм цели от личния живот, или само работни?

---

## Всекидневно адаптиране

Седмичният план не е свещен документ. Той е компас, не клетка.

Реалността рядко следва плановете точно. Обажда се клиент с проблем. Среща се отменя. Дете се разболява. Идея, която не можеш да отложиш.

Всекидневното планиране служи именно за това: да адаптираш реакциите си спрямо седмичната картина. Когато знаеш какво е приоритетно за седмицата, е много по-лесно да решиш дали неочакваното събитие трябва да измести нещо важно — или може да изчака.

Без седмична рамка всяко отклонение изглежда важно. С нея — имаш с какво да го сравниш.

---

## Схемата накратко

Ако трябва да обобщим системата в един ред:

**Изявление за мисия → Роли → Цели → График → Дневни планове → Делегиране**

Всяка стъпка е свързана с предишната. Ролите произтичат от ценностите и мисията. Целите произтичат от ролите. Графикът — от целите. А дневното планиране е просто фина настройка на вече съществуваща структура.

Ключовите критерии, които се определят за добро седмично планиране, са: **съгласуваност** между цели, приоритети и действия, **баланс** между всички роли, **фокус** върху приоритетите, **човешкото измерение** — планирането не е само за задачи, понякога има обстоятелства които изискват графика да се съобразява с други хора, **гъвкавост** — планът се адаптира така че да ни бъде слуга, а не господар, и **преносимост** — така че системата да работи навсякъде, не само в офиса.

---

Когато за пръв път прекарах неделя в планиране на седмицата, не видях незабавни резултати. Но след няколко седмици нещо се промени — не в количеството свършена работа, а в качеството на решенията, които взимах. Спрях да реагирам. Започнах да избирам.

Седмичното планиране не е техника за продуктивност. То е навик за яснота.

Ако не знаеш откъде да започнеш — започни с ролите. Вземи лист и напиши кой си. Не какво правиш — кой си. Оттам нататък всичко останало идва естествено.`;

async function update() {
  const q = query(collection(db, 'blogArticles'), where('slug', '==', 'kak-da-planirash-sedmicata-si'));
  const snapshot = await getDocs(q);
  if (snapshot.empty) { console.error('❌ Статията не е намерена.'); process.exit(1); }
  await updateDoc(snapshot.docs[0].ref, { content: updatedContent, updatedAt: Timestamp.now() });
  console.log('✅ Статията е обновена в Firebase.');
  process.exit(0);
}

update().catch(err => { console.error('❌ Грешка:', err); process.exit(1); });
